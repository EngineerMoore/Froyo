/* Create a prompt where users list out froyo flavors seperated by commas 
Ex. "vanilla,vanilla,vanilla,strawberry,coffee,coffee."*/
// Seperate the input into an array of strings
// Loop through through the array to find the flavors(?)
// Create an object that lists how many of each flavor was ordered
// Create a function to that returns an object
  // the function should contain the array used to count flavor events
// Make the object visible on the console